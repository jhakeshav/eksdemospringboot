apiVersion: apps/v1
kind: Deployment
metadata:
  name: tagfact-springboot
spec:
  replicas: 3
  selector:
    matchLabels:
      app: tagfact-springboot
  template:
    metadata:
      labels:
        app: tagfact-springboot
    spec:
      containers:
        - name: springboot
          image: keshav/tagfact-docker-demo:tagfact-docker-demo
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: tagfact-springboot
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: tagfact-springboot
